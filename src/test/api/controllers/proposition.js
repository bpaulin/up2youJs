var should = require('should');
var request = require('supertest');
var server = require('../../../app');

describe('proposition', function () {

  describe('GET /proposition', function () {

    xit('should work', function (done) {
      done();
    });

  });

});
